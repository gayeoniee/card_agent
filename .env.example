# card_agent 환경변수. serve.py 의 ENV_* 상수와 반드시 같이 고친다.

# 카드 원화 폴더 (git 밖에 둔다)
CARD_AGENT_ASSETS=templates/cards
# 결과가 쌓이는 폴더
CARD_AGENT_OUT=out
# 바깥에 직접 열지 않는다. compose 가 앞에서 받는다
CARD_AGENT_HOST=127.0.0.1
CARD_AGENT_PORT=8099
# 화풍 이름 (src/pixelize.py 의 STYLES)
CARD_AGENT_STYLE=pixel
